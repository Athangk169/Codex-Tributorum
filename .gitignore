# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
.env

# Python bytecode cache (auspex_daemon.py)
__pycache__/
*.pyc

# Android release-signing secrets — never check these in.
android/app/keystore.properties
android/app/*.keystore
android/app/*.jks
android/keystore.properties
*.keystore
*.jks

# Local tailscale serve backups — snapshot at migration time, not
# meaningful outside that one machine. Live state lives in
# `tailscale serve status`.
tailscale-serve*.backup.json
tailscale-serve*.json