# Copy to .env and fill in. NEVER commit .env (it's gitignored).
COUCHDB_ADMIN_USER=admin
COUCHDB_ADMIN_PASSWORD=change-me

# Optional: Uptime Kuma push-monitor URL for the daemon heartbeat
KUMA_PUSH_URL=
